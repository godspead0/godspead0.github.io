# ============ 依赖与构建产物（可随时重新生成）============
node_modules/
/dist/
.vite/
npm-debug.log*

# ============ 旧 VitePress 站点遗留（已被 notes-manager 取代，仅保留在本地）============
/.vitepress/
/index.md
/教程.md
/package.json
/package-lock.json
/assets/
/godspead0.github.io/

# ============ 笔记数据（不在本仓库）============
# 笔记存放在**独立的私有仓库**里，本地克隆位于本目录**之外**，由「提交笔记.bat」提交推送。
# 这里刻意不写出具体仓库名 —— 本仓库是公开的，写了等于把私有仓库名泄露给所有人。
# ⚠️ 切勿把笔记仓库克隆到本目录内：本站点仓库是公开的，
#    而「提交笔记.bat」第 3 步会在此目录执行 git add -A，一旦放进来就会被推到公开仓库。

# ============ 本机私有文件（含私有仓库名，绝不提交）============
/我的仓库配置（本机私有）.md

# ============ 系统 / 编辑器文件 ============
.DS_Store
Thumbs.db
.vscode/
.obsidian/
