# 操作手册

> 个人在线笔记收纳与打卡系统 · 完整使用流程
> 线上地址：<https://godspead0.github.io/>

> **关于占位符**：本文档中的 `your-name` / `my-tech-notes` / `my-algo-notes` /
> `my-algo-notes-dir` 都是**占位符，不是真实仓库名**（公开仓库的文档不应泄露私有仓库名）。
> 真实值见本机私有文件「我的仓库配置（本机私有）.md」，或直接看你浏览器里已保存的配置。
>
> 下表中凡是占位符的位置，**照着换成你的真实值**即可。

---

## 速查卡

| 我要…… | 去哪里 |
| --- | --- |
| 写技术笔记 | `D:\vscode_test_all\my-tech-notes\全栈\` |
| 写算法笔记 | `D:\vscode_test_all\my-algo-notes-dir\笔记\` |
| 提交到 GitHub | 双击 `D:\vscode_test_all\tech_doc\提交笔记.bat`（4 步：技术 → 算法 → 公开镜像 → 站点） |
| 看最新笔记 | 网站右上角 **同步** |
| 切技术/算法 | 左侧栏 **仓库** |
| 备份全部 | 网站右上角 **导出** |
| 分享给别人 | 直接把 <https://godspead0.github.io/> 发过去，**对方什么都不用配** |
| 看数据来自哪 | 顶栏最右侧的滑块图标 →「数据来源」 |

---

## 一、配置：不需要

**网站是纯只读的，没有任何需要填写的东西。**

- 访客：打开 <https://godspead0.github.io/> 直接看，**不用登录、不用 Token**
- 你自己：也一样 —— 网站上不能新建 / 编辑 / 打卡，所以没有可配置的东西

数据源固定在代码里（`notes-manager/src/composables/useConfig.js`）：

```js
const PUBLIC_OWNER  = 'godspead0'
const PUBLIC_REPO   = 'godspead0_notes1'
const PUBLIC_BRANCH = 'main'
```

页面向 GitHub 只发**读**请求，不在浏览器里保存任何凭据 ——
没有 Token 输入框、没有 `localStorage` 凭据、没有“测试连接”。

> 想换仓库？改上面三个常量（**必须是公开仓库**，否则访客读不到内容），
> 然后把站点源码推送上去，Actions 会自动重新部署。

**那内容怎么更新？** 见下一节 —— 全部在本地完成。

---

## 二、日常使用（每天的主流程）

### 2.1 写笔记

| 笔记类型 | 打开这个文件夹 | 分类怎么来 |
| --- | --- | --- |
| 技术 | `D:\vscode_test_all\my-tech-notes\全栈\` | 一级子文件夹名 = 分类，如 `前端部分\Vue.md` → 分类「前端部分」 |
| 算法 | `D:\vscode_test_all\my-algo-notes-dir\笔记\` | 同上，如 `力扣\二分查找.md` → 分类「力扣」 |

- 用 Obsidian / VS Code / 记事本都行，就是普通的 `.md` 文件
- 放在笔记目录根下也认（会归入「未分类」）
- 最多递归 6 层子目录
- 标题优先取正文里的 `# 标题`；正文为空则用文件名

> ⚠️ **网站上不能写笔记** —— 页面没有新建 / 编辑入口。
> 所有内容都在本地写好，再用下面的脚本提交。

### 2.2 提交到 GitHub

双击 `D:\vscode_test_all\tech_doc\提交笔记.bat`

```text
请输入本次提交说明（直接回车使用默认）：写完 MySQL 索引
```

然后它会自动跑四步（一次输入，四步共用这条说明）：

```text
[1/4] 同步技术笔记 ...
      [完成] 技术笔记 已推送。
[2/4] 同步算法笔记 ...
      [完成] 算法笔记 已推送。
[3/4] 同步公开展示仓库 ...
      全栈 → 94 篇
      镜像完成：技术 94 篇、算法 0 篇。
      [完成] 公开展示仓库已推送。
[4/4] 检查站点源码 ...
```

- **第 3 步是给别人看的关键**：它把两个私有工作区里的 `.md` **单向**复制到
  `D:\vscode_test_all\notes_public`（公开展示仓库）并推送。网站读的就是它
- 镜像**只复制 `.md`**：`.cpp` / `.prob` / `origin/` / `.obsidian/` 都不会进公开仓库
- 镜像**会同步删除**：你在本地删掉的笔记，也会从公开仓库移除
- `checkins.json` / `categories.json` 属于网站数据，镜像脚本**绝不触碰**
- ⚠️ **不要手改 `notes_public` 里的笔记** —— 下次跑镜像会被私有工作区的内容覆盖。
  要改笔记，改私有工作区里的原文
- 某个仓库**没改动**会显示「没有检测到改动，跳过」，属正常
- 每步内部都是 `git add -A` → `commit` → `pull --rebase` → `push`
- 出现冲突会停下来并打印具体命令，按提示手动处理即可
- 网络抖动会自动重试（`github.com:443` 常被拦，remote 已全部改用 SSH）
- 跑完停在 `按任意键继续`，关掉窗口即可

### 2.3 网站上看最新笔记

回到网站，点右上角 **⟳ 同步**（约 1-3 秒）。
网站直连 GitHub 读取数据，**不需要**等站点部署完成。

> 点「同步」会**绕过两层缓存**（文件树 60 秒缓存 + raw CDN 的 5 分钟缓存），
> 所以刚推上去的笔记立刻就能看到。
> 而平时打开网页走的是缓存（快，不重复下载正文）—— 也就是说
> **刷新页面**不一定能看到刚推的笔记，**点「同步」才一定行**。

### 2.4 打卡热力图（只读）

右侧仪表盘的热力图数据来自公开仓库根目录的 `checkins.json`。

> ⚠️ **网站上不能打卡** —— 打卡 / 撤销 / 补卡按钮已全部移除，页面不写任何数据。
> 热力图只负责**展示**已有记录；没有 `checkins.json` 时显示为空。
>
> 想补记录的话，直接在 GitHub 上编辑公开仓库根目录的 `checkins.json`：
> `{ "2026-09-19": 2, "2026-09-18": 1 }`（键是日期，值是当天次数），改完回网站点「同步」。

---

## 三、找笔记（侧边栏 + 搜索）

左侧栏从上到下：

```text
全部笔记            ← 清空所有筛选
当前筛选            ← 有筛选时才出现，点「清除筛选」一键还原
仓库                ← 技术 / 算法（一级分类），点一下只看这个库
分类                ← 文件夹名，随所选仓库联动
标签                ← 标签云，多选为 AND（同时满足）
时间线归档          ← 2026年9月 (4)，可折叠展开到具体笔记
```

**筛选可以叠加**：先点「算法」→ 再点分类「洛谷」→ 再看某个月份。

**搜索**：顶部搜索框**边打边筛**（不用回车）；右侧的 **搜索按钮**点击后会把光标
聚焦回输入框，方便继续改词；命中数量显示在按钮旁（如 `3 条`）。
按 `/` 或 `Ctrl+K` 也可直接聚焦搜索框。

右侧仪表盘显示：笔记总数、正文字数、活跃天数、累计打卡、最长连续打卡、打卡热力图。
这些数字全部由公开仓库里的数据算出来，页面只读不算错。

---

## 四、其它操作

| 想做什么 | 怎么做 |
| --- | --- |
| 看一篇笔记 | 点卡片 → 阅读页（渲染 Markdown，带目录锚点） |
| 下载单篇 | 卡片悬停 → ⬇；阅读页底部也有「下载 .md」 |
| 复制原文 | 阅读页底部 →「复制原文」（含 frontmatter 的完整 Markdown） |
| 只下载一部分 | 列表里勾选多篇 →「导出所选 zip」或「逐篇下载」 |
| 全站备份 | 右上角 **导出** → 打包 `zip`（含全部笔记 + `checkins.json` + `categories.json`） |
| 看数据来源 | 顶栏最右侧滑块图标 →「数据来源」 |
| 换深浅色 | 右上角 ☀ / 🌙（默认跟随系统） |
| 改 / 删一篇笔记 | ⚠️ **网站上做不了**。去本地笔记目录改，然后跑 `提交笔记.bat` |
| 换设备 | 什么都不用做 —— 没有凭据要迁移，打开网站就能看 |

---

## 五、排错对照表

| 现象 | 原因与处理 |
| --- | --- |
| 算法一篇都没有 | `my-algo-notes-dir\笔记\` 里还没有 `.md`；写一篇进去再跑 `提交笔记.bat` |
| 某一类笔记不出现 | 检查公开展示仓库里是否有对应子目录：技术是 `全栈/`，算法是 `算法/` |
| 笔记数量对不上 | 只统计 `.md` / `.markdown`；`.cpp` / `.prob` 等代码文件不会被当成笔记 |
| 本地删了笔记，网站还在 | 还没跑 `提交笔记.bat` 第 3 步（镜像带删除同步），或网站没点「同步」 |
| 本地删了笔记，网站没了但本地又回来了 | 不可能 —— 镜像是单向的，不会往私有工作区写回任何东西 |
| 热力图一直是空的 | 公开仓库根目录没有 `checkins.json`，或里面没有数据（详见 §2.4） |
| 网站提示「读取失败（HTTP 403）」 | 匿名 API 额度用尽（60 次/小时/IP）。等一小时，或换个网络 |
| 网站提示「仓库不存在或不是公开的」 | 公开展示仓库被改成私有（匿名读会 404），或 `useConfig.js` 里的 `PUBLIC_*` 常量写错了。**站点不会静默显示 0 篇笔记**，而是直接把这条错报出来 |
| 单篇读取失败 | 文件 >1MB，前端会自动改走 raw 通道重试 |
| 网站打不开 / 样式乱 | 刷新页面（`Ctrl + F5`） |

---

## 六、数据结构速查

### 6.1 两套仓库

**公开展示仓库**（网站对所有人展示的就是它，`public`）：

| | 值 |
| --- | --- |
| 仓库 | `godspead0_notes1` |
| 本地克隆 | `D:\vscode_test_all\notes_public` |
| 分支 | `main` |
| 内容 | `全栈/`（技术笔记副本）+ `算法/`（算法笔记副本）+ `checkins.json` |
| 谁维护 | **只有** `提交笔记.bat` 第 3 步镜像 `.md`；网站只读，不写任何东西 |

**私有工作区**（你本地编辑用，网站不直接读）：

| | 技术 | 算法 |
| --- | --- | --- |
| 仓库 | `your-name/my-tech-notes`（私有） | `your-name/my-algo-notes`（私有） |
| 本地克隆 | `D:\vscode_test_all\my-tech-notes` | `D:\vscode_test_all\my-algo-notes-dir` |
| 分支 | `master` | `main` |
| 笔记位置 | `全栈/`（递归 6 层） | **`笔记/`**（递归 6 层） |
| 网站里的一级分类 | 技术 | 算法 |

> ⚠️ **别手改 `notes_public` 里的笔记**：下次跑 `提交笔记.bat` 会被私有工作区的内容覆盖。
> 要改笔记，改私有工作区里的原文。
>
> 算法的「笔记位置」是 `笔记/` 而不是仓库根目录 —— 因为算法仓库根下还有
> `力扣/` `洛谷/` `acwing/` 等**代码**目录（里面是 `.cpp`），
> 把笔记目录限定在 `笔记/` 可以避免误扫。

### 6.2 文件命名与元数据

```text
{id}_{slug}.md          # 网站里新建的笔记（id = 13 位毫秒时间戳，slug 保留中文）
checkins.json           # { "YYYY-MM-DD": count, ... }
categories.json         # 分类 / 标签配色元数据（可选）
```

> 这两个 JSON 存放在**公开展示仓库的根目录**，由镜像脚本之外的途径维护
> （手工编辑或其它脚本）；镜像脚本不会动它们。
> `{id}_{slug}.md` 这种命名是历史遗留 —— 现在网站上不新建笔记了，
> 但旧的这类文件名仍能正常解析。

### 6.3 网站展示哪些内容

| | 技术分类 | 算法分类 |
| --- | --- | --- |
| 指向仓库 | `godspead0_notes1` | `godspead0_notes1` |
| 「笔记目录」设置 | `全栈` | `算法` |
| 实际扫描范围 | **仅 `全栈/` 子树内** | **仅 `算法/` 子树内** |
| 展示的文件类型 | 只展示 `.md` / `.markdown` | 只展示 `.md` / `.markdown` |

- 两个分类限定在各自的子目录里，**互不重叠**，不会重复显示
- `.cpp`、`.prob`、`.json`、`.png`、`.obsidian/` —— **都不会展示**
- 想加一个新分类，在私有工作区里建子目录（如 `全栈/新分类/xx.md`），
  跑一次 `提交笔记.bat` 就会自动镜像过去

### 6.4 分类与标签的来源

- **分类** = 笔记所在的一级子文件夹名（技术：`全栈/前端部分/Vue.md` → 「前端部分」）
- **标签** = 笔记 frontmatter 里的 `tags`
- 两者都**只看不改**：分类来自文件夹结构，标签来自笔记内容。
  想调整分类就把文件挪到别的文件夹，想加标签就改 frontmatter，然后跑 `提交笔记.bat`

---

## 七、安全与隐私

> **你的笔记是公开的。** 这是刻意的设计 —— 你要把笔记分享给别人看。
> 同时，网站**不持有任何凭据**，所以也不存在"凭据被偷"这条攻击链。

### 谁能看到 / 能做什么

| | 任何拿到链接的人 | 你 |
| --- | --- | --- |
| 读笔记 | ✅ 公开展示仓库里的全部 `.md` | ✅ 同样 |
| 看打卡记录 | ✅ `checkins.json`（日期与次数） | ✅ 同样 |
| 新建 / 编辑 / 删除 / 打卡 | ❌ **页面没有这些入口** | ❌ 网站上也做不了；在本地改 |
| 私有工作区仓库 | ❌ GitHub 上匿名访问 404（仍是私有） | ✅ 本地随便改 |

### 三层设计

1. **私有工作区**（`my-tech-notes` / `my-algo-notes`）**仍然私有**，
   里面有 `origin/` 的 `.cpp`、`.vs` 工程等额外文件，匿名访问 404
2. **公开展示仓库**（`godspead0_notes1`）是 public 的，**只放 `.md` 笔记副本**，
   由 `提交笔记.bat` 第 3 步单向镜像过去
3. **网站完全没有写能力**：不是"按钮禁用"，而是新建 / 编辑 / 删除 / 导入 / 打卡的
   代码路径已从应用层移除；也不存在 Token 输入框或 `localStorage` 凭据

### 但要注意这几点

| 风险 | 说明 |
| --- | --- |
| **⚠️ 笔记公开且不可逆** | 搜索引擎会收录，别人能 fork / 复制。**所有放进 `全栈/` 或 `笔记/` 的内容都会公开** —— 没有「只本地留一份」的目录。真想藏起来的内容，别放进这两个目录 |
| **不要手改 `notes_public`** | 下次跑镜像会被私有工作区的内容覆盖；改笔记请改私有工作区的原文 |
| **别把笔记放进站点目录** | `tech_doc` 是公开仓库，`提交笔记.bat` 第 4 步会在那里执行 `git add -A` |
| **共用电脑** | 只读站点不保存凭据，别人只能看到你本来就公开的笔记。用完点浏览器「清除站点数据」可顺手清掉本地缓存（纯粹为了不留痕） |
| **提交历史带邮箱** | ~~公开仓库的每次提交都会公开你的 Git 邮箱~~ → **已处理，见下方「邮箱匿名化」** |

### 邮箱匿名化（已完成）

原先公开仓库的提交带真实邮箱。现已全部改为 GitHub 匿名邮箱：

```bat
:: 已在本仓库、notes_public、my-tech-notes、my-algo-notes-dir 四处的仓库级配置里设好
git config user.email "174760772+godspead0@users.noreply.github.com"
git config user.name  "zhongrongwei"
```

**已完成的操作**：用 `git filter-branch --env-filter` 改写历史 → 强制推送。
改写后 `git diff` 验证**文件内容零变化**，只动了提交元数据。
镜像脚本每次运行都会**无条件**重设这两个仓库级配置（不能用「为空才设」判断 ——
git config 读的是含全局配置的生效值，会漏掉真实邮箱）。

**仍有残留（重要）**：GitHub 会保留被"抛弃"的旧提交，**通过旧的 SHA 仍能直接打开**，
例如 `github.com/<你的账号>/godspead0.github.io/commit/<旧 SHA>`。
不过：

- 分支的正常浏览路径（Commits 页面、`git log`、克隆）**已完全看不到旧提交** ✅
- 旧 SHA 不对外公开，只有当时看过仓库的人才可能知道
- 要**彻底**清除只有两条路：① 请 GitHub Support 清理缓存提交；② 删除并重建该仓库

> 另外建议去 GitHub → Settings → Emails 勾选
> **Keep my email addresses private** 和 **Block command line pushes that expose my email**，
> 从源头防止再次泄露。

### 网络：一律走 SSH over 443

实测 `github.com:443`（20.205.243.166）**直连被阻断且是间歇性的**，
而 `api.github.com`、`raw.githubusercontent.com` 通畅。表现为推送报
`Failed to connect to github.com port 443 after 21096 ms`。

**解决办法：把 github.com 映射到 GitHub 官方的备用入口 `ssh.github.com:443`。**
`~/.ssh/config` 已配好：

```
Host github.com
  HostName ssh.github.com
  Port 443
  User git
  IdentityFile ~/.ssh/id_ed25519
```

验证：

```bat
ssh -T git@github.com
:: Hi godspead0! You've successfully authenticated, but GitHub does not provide shell access.
```

四个仓库的 remote 都已改成 SSH 形式：

```
git@github.com:godspead0/godspead0.github.io.git
git@github.com:godspead0/godspead0_understand.git
git@github.com:godspead0/godspead0_algorithm.git
git@github.com:godspead0/godspead0_notes1.git
```

`提交笔记.bat` 与镜像脚本对网络失败都带多次重试，并且**保留**了本机代理
`http://127.0.0.1:7892` 作为 HTTPS 回退（SSH 走不到时才用得上）。

---

## 八、相关文件位置

| 内容 | 路径 |
| --- | --- |
| 站点源码仓库 | `D:\vscode_test_all\tech_doc`（公开，推 `main`） |
| SPA 源码 | `D:\vscode_test_all\tech_doc\notes-manager` |
| 一键提交脚本 | `D:\vscode_test_all\tech_doc\提交笔记.bat` |
| 镜像脚本 | `D:\vscode_test_all\tech_doc\镜像到公开仓库.ps1` |
| 公开展示仓库本地克隆 | `D:\vscode_test_all\notes_public` |
| 离线自测 | `cd notes-manager && npm run smoke`（157 项断言，无需浏览器） |
| 本机私有配置备忘 | `D:\vscode_test_all\tech_doc\我的仓库配置（本机私有）.md`（已 gitignore） |

> 改了网站源码后需要重新构建部署，推送到 `main` 后由 GitHub Actions 自动发布（约 1-2 分钟）。
> 只改笔记的话，点网站「同步」即可立刻看到，无需等待。
