# 操作手册

> 个人在线笔记收纳与打卡系统 · 完整使用流程
> 线上地址：<https://godspead0.github.io/>

> **关于占位符**：本文档中的 `your-name` / `my-tech-notes` / `my-algo-notes` /
> `my-algo-notes-dir` 都是**占位符，不是真实仓库名**（公开仓库的文档不应泄露私有仓库名）。
> 真实值见本机私有文件「我的仓库配置（本机私有）.md」，或直接看你浏览器里已保存的配置。
>
> 下表中凡是占位符的位置，**照着换成你的真实值**即可。

---

## 速查卡

| 我要…… | 去哪里 |
| --- | --- |
| 写技术笔记 | `D:\vscode_test_all\my-tech-notes\全栈\` |
| 写算法笔记 | `D:\vscode_test_all\my-algo-notes-dir\` |
| 提交到 GitHub | 双击 `D:\vscode_test_all\tech_doc\提交笔记.bat`（4 步：技术 → 算法 → 公开镜像 → 站点） |
| 看最新笔记 | 网站右上角 **同步** |
| 今日打卡 | 网站右上角 🔥（每天一次，需先填 Token） |
| 切技术/算法 | 左侧栏 **仓库** |
| 备份全部 | 网站右上角 **导出** |
| 分享给别人 | 直接把 <https://godspead0.github.io/> 发过去，**对方什么都不用配** |

---

## 一、配置（几乎不用做）

**访客什么都不用填**：打开 <https://godspead0.github.io/> 直接就能看到全部笔记。

**只有你自己要写笔记 / 打卡**，才需要填一次 Token（每台设备一次）。

### 1.1 创建一个只授权公开展示仓库的 Token

1. 浏览器打开 <https://github.com/settings/personal-access-tokens/new>
2. **Token name**：填 `notes-manager`
3. **Expiration**：建议 1 年（到期需重建）
4. **Repository access** → 选 **Only select repositories** → 勾选：
   - ✅ `godspead0_notes1`（公开展示仓库）
5. **Permissions** → 展开 **Repository permissions** → 找到 **Contents** → 设为 **Read and write**
6. 拉到底点 **Generate token** → 复制那串 `github_pat_...`

> ⚠️ Token 只显示一次，请自己保存好。**不要**提交进任何仓库文件、也不要发给别人——
> 它只存在浏览器本地 `localStorage` 里。
>
> 只需要写权限的话，**不必**授权那两个私有工作区仓库 —— 网站读的是公开展示仓库。

### 1.2 在网站里粘贴 Token

打开 <https://godspead0.github.io/>，点右上角 **⚙ 连接设置**。

弹窗顶部有 **技术 / 算法** 两个页签。它们的 Owner / Repo / Branch / 笔记目录**已经预填好**
—— 指的是同一个公开展示仓库，本来就是公开信息，**保持不动**即可。

你只需要在 **Token** 那一栏粘贴同一串，两个页签各点一次 **「测试并保存」**，看到绿色的：

```text
✅ 连接正常
仓库：godspead0/godspead0_notes1（公开） · 分支：main · 写入权限：可写 ✅
```

才算成功。**若显示「只读 ❌」**，说明第 5 步 Contents 权限没设成 Read and write，回 GitHub 改 Token。

两个页签都测通后，点右上角 **✕** 关闭，网站会自动开始同步。

> 没填 Token 时网站处于**只读模式**（顶栏带「只读」徽标）：能看不能改。
> 这正是访客看到的状态 —— 打开链接就能读全部笔记，但改不了任何东西。

---

## 二、日常使用（每天的主流程）

### 2.1 写笔记——三选一

**方式 A：用 Obsidian 本地写（推荐）**

| 笔记类型 | 打开这个文件夹 | 分类怎么来 |
| --- | --- | --- |
| 技术 | `D:\vscode_test_all\my-tech-notes\全栈\` | 一级子文件夹名 = 分类，如 `前端部分\Vue.md` → 分类「前端部分」 |
| 算法 | `D:\vscode_test_all\my-algo-notes-dir\` | 同上，如 `力扣\二分查找.md` → 分类「力扣」 |

- 放在笔记目录根下也认（会归入「未分类」）
- 最多递归 6 层子目录
- 标题优先取正文里的 `# 标题`；正文为空则用文件名

**方式 B：在网站里写**

点右上角 **+ 新建** → 填标题 / 分类 / 标签 / 正文 →
注意有一栏 **「保存到仓库」**，下拉可选 `技术` 或 `算法`（决定这篇写进哪个库）→
点 **创建并同步**。

**方式 C：导入已有 `.md`**

点 **导入** → 选文件，或直接把整个文件夹拖进窗口。会写进当前选中的仓库。

### 2.2 提交到 GitHub

双击 `D:\vscode_test_all\tech_doc\提交笔记.bat`

```text
请输入本次提交说明（直接回车使用默认）：写完 MySQL 索引
```

然后它会自动跑四步（一次输入，四步共用这条说明）：

```text
[1/4] 同步技术笔记 ...
      [完成] 技术笔记 已推送。
[2/4] 同步算法笔记 ...
      [完成] 算法笔记 已推送。
[3/4] 同步公开展示仓库 ...
      全栈 → 94 篇
      镜像完成：技术 94 篇、算法 0 篇。
      [完成] 公开展示仓库已推送。
[4/4] 检查站点源码 ...
```

- **第 3 步是给别人看的关键**：它把两个私有工作区里的 `.md` 复制到
  `D:\vscode_test_all\notes_public`（公开展示仓库）并推送。网站读的就是它
- 镜像**只复制 `.md`**：`.cpp` / `origin/` / `.obsidian/` 都不会进公开仓库
- 镜像**会同步删除**：你在本地删掉的笔记，也会从公开仓库移除
- `checkins.json` / `categories.json` 由网站直接写入公开仓库，镜像脚本**绝不触碰**
- 某个仓库**没改动**会显示「没有检测到改动，跳过」，属正常
- 每步内部都是 `git add -A` → `commit` → `pull --rebase` → `push`
- 出现冲突会停下来并打印具体命令，按提示手动处理即可
- 网络抖动会自动重试（`github.com:443` 常被拦，remote 已全部改用 SSH）
- 跑完停在 `按任意键继续`，关掉窗口即可

### 2.3 网站上看最新笔记

回到网站，点右上角 **⟳ 同步**（约 1-3 秒）。
网站直连 GitHub 读取数据，**不需要**等站点部署完成。

### 2.4 打卡

右上角 🔥 按钮：

- **点一下** = 今日打卡 +1，按钮变绿显示「已打卡」；**每天只允许一次**（之后按钮置灰）
- **不用手动点**：当天首次新建 / 修改 / 导入笔记会自动 +1，
  并提示「已自动打卡 +1（笔记已创建）」
- 撤销 / 补卡在右侧仪表盘：可撤销今日打卡；点热力图上某一天可给**过去的某天补卡**
  （今天不允许重复补）

---

## 三、找笔记（侧边栏 + 搜索）

左侧栏从上到下：

```text
全部笔记            ← 清空所有筛选
当前筛选            ← 有筛选时才出现，点「清除筛选」一键还原
仓库                ← 技术 / 算法（一级分类），点一下只看这个库
分类                ← 文件夹名，随所选仓库联动
标签                ← 标签云，多选为 AND（同时满足）
时间线归档          ← 2026年9月 (4)，可折叠展开到具体笔记
```

**筛选可以叠加**：先点「算法」→ 再点分类「洛谷」→ 再看某个月份。

**搜索**：顶部搜索框**边打边筛**（不用回车）；右侧的 **搜索按钮**点击后会把光标
聚焦回输入框，方便继续改词；命中数量显示在按钮旁（如 `3 条`）。
按 `/` 或 `Ctrl+K` 也可直接聚焦搜索框。

右侧仪表盘显示：笔记总数、正文字数、最长连续打卡、今日打卡、打卡热力图。

---

## 四、其它操作

| 想做什么 | 怎么做 |
| --- | --- |
| 改一篇笔记 | 点卡片上的 ✏️；或在卡片上打开阅读页后点编辑 |
| 删除 | 卡片悬停 → 🗑；或列表里勾选多篇后批量删除 |
| 下载单篇 | 卡片悬停 → ⬇ |
| 全站备份 | 右上角 **导出** → 打包 `zip`（含全部笔记 + `checkins.json` + `categories.json`） |
| 换深浅色 | 右上角 ☀ / 🌙 |
| 换设备 | 新设备需重新填一次 Token（凭据存在浏览器本地，不同步） |
| 换 Token | 连接设置 → 对应页签改 Token → 测试并保存 |
| 彻底清除本机数据 | 连接设置 → 左下「清除凭据」→ 确认。会删掉 Token **以及本机缓存的笔记正文与打卡记录**（GitHub 上的笔记不受影响）。**在公共电脑用完请务必点它。** |

---

## 五、排错对照表

| 现象 | 原因与处理 |
| --- | --- |
| 「Token 无效或已过期」 | Token 被吊销 / 复制不全 / 过期。重建并重填**两个页签** |
| 「仓库或路径不存在」 | fine-grained Token **没勾选**那个仓库时 GitHub 返回 404（不是 403）。回 Token 设置把两个仓库都勾上 |
| 显示「只读 ❌」 | Contents 权限不是 Read and write |
| 算法仓库一篇都没有 | `my-algo-notes-dir` 里目前只有 `.cpp` 没有 `.md`，所以是空的；写一篇 `.md` 进去，或在网站切到「算法」后新建 |
| 某一类笔记不出现 | 检查该仓库页签的「笔记目录」：技术必须是 `全栈`，算法必须**留空** |
| 笔记数量对不上 | 只统计 `.md` / `.markdown`；两个仓库里的代码文件不会被当成笔记 |
| 打卡点不动 | 今天已经打过了（每天限一次）；补卡请在热力图上选过去的日期 |
| 保存冲突 409 | 同一文件在别处被改过。点「同步」拉最新版再改 |
| 单篇读取失败 | 文件 >1MB，前端会自动改走 `download_url` raw 通道 |
| 网站打不开 / 样式乱 | 刷新页面（`Ctrl + F5`） |

---

## 六、数据结构速查

### 6.1 两套仓库

**公开展示仓库**（网站对所有人展示的就是它，`public`）：

| | 值 |
| --- | --- |
| 仓库 | `godspead0_notes1` |
| 本地克隆 | `D:\vscode_test_all\notes_public` |
| 分支 | `main` |
| 内容 | `全栈/`（技术笔记副本）+ `算法/`（算法笔记副本）+ `checkins.json` |
| 谁维护 | `提交笔记.bat` 第 3 步镜像 `.md`；网站写入 `checkins.json` |

**私有工作区**（你本地编辑用，网站不直接读）：

| | 技术 | 算法 |
| --- | --- | --- |
| 仓库 | `your-name/my-tech-notes`（私有） | `your-name/my-algo-notes`（私有） |
| 本地克隆 | `D:\vscode_test_all\my-tech-notes` | `D:\vscode_test_all\my-algo-notes-dir` |
| 分支 | `master` | `main` |
| 笔记位置 | `全栈/`（递归 6 层） | 仓库根目录（递归 6 层） |
| 网站里的一级分类 | 技术 | 算法 |

> ⚠️ **别手改 `notes_public` 里的笔记**：下次跑 `提交笔记.bat` 会被私有工作区的内容覆盖。
> 要改笔记，改私有工作区里的原文。

### 6.2 文件命名与元数据

```text
{id}_{slug}.md          # 网站里新建的笔记（id = 13 位毫秒时间戳，slug 保留中文）
checkins.json           # { "YYYY-MM-DD": count, ... }   ← 自动创建
categories.json         # 分类 / 标签配色元数据          ← 自动创建
```

> `checkins.json` 与 `categories.json` 存放在**主仓库（公开展示仓库）的根目录**，
> 由网站直接读写；镜像脚本不会动它们。

### 6.3 网站只展示什么

| | 技术页签 | 算法页签 |
| --- | --- | --- |
| 指向仓库 | `godspead0_notes1` | `godspead0_notes1` |
| 「笔记目录」设置 | `全栈` | `算法` |
| 实际扫描范围 | **仅 `全栈/` 子树内** | **仅 `算法/` 子树内** |
| 展示的文件类型 | 只展示 `.md` / `.markdown` | 只展示 `.md` / `.markdown` |

- 因为两个页签都限定在各自的子目录里，**互不重叠**，不会重复显示
- `.cpp`、`.prob`、`.json`、`.png`、`.obsidian/` —— **都不会展示**
- 想加一个新分类，在公开仓库里建子目录即可（如 `全栈/新分类/xx.md`）；
  更方便的做法是在私有工作区建于目录，跑一次 bat 自动镜像过去

### 6.4 分类与标签的来源

- **分类** = 笔记所在的一级子文件夹名（技术：`全栈/前端部分/Vue.md` → 「前端部分」）
- **标签** = 笔记 frontmatter 里的 `tags`，或在网站编辑器里添加
- 两者都可以在网站里改，改完会自动写回对应仓库

---

## 七、安全与隐私

> **现状：你的笔记是公开的。** 这是刻意的设计 —— 你要把笔记分享给别人看。

### 谁能看到 / 能做什么

| | 匿名访客（拿到链接的人） | 你自己（填了 Token） |
| --- | --- | --- |
| 读笔记 | ✅ 公开展示仓库里的全部 `.md` | ✅ 同样 |
| 看打卡记录 | ✅ `checkins.json`（日期与次数） | ✅ 同样 |
| 新建 / 编辑 / 删除 / 打卡 | ❌ 按钮禁用，**服务层也会拒绝写** | ✅ |
| 私有工作区仓库 | ❌ GitHub 上匿名访问 404（仍是私有） | ✅ 本地随便改 |

### 三层设计

1. **私有工作区**（`my-tech-notes` / `my-algo-notes`）**仍然私有**，
   里面有 `origin/` 的 `.cpp`、`.vs` 工程等额外文件，匿名访问 404
2. **公开展示仓库**（`godspead0_notes1`）是 public 的，**只放 `.md` 笔记副本**，
   由 `提交笔记.bat` 第 3 步单向镜像过去
3. 访客**没有 Token**：写操作在服务层就被拦下（返回「当前是只读模式」），
   不是只把按钮藏起来 —— 前端禁用只是提示，真正的闸门在 `saveFile` / `deleteFile`

### 但要注意这几点

| 风险 | 说明 |
| --- | --- |
| **⚠️ 笔记已经公开，且不可逆** | 搜索引擎会收录，别人能 fork / 复制。**当前设计下所有笔记都会公开** —— 没有「只本地留一份」的目录。真想藏起来的笔记，别放进 `全栈/` 或算法仓库 |
| **不要手改 `notes_public`** | 下次跑镜像会被私有工作区的内容覆盖；改笔记请改私有工作区的原文 |
| **共用电脑** | 谁用了你的浏览器 = 拿到你的 Token = 能改能删。用完点「清除凭据」——它会同时抹掉 Token、**笔记正文缓存**和打卡缓存 |
| **Token 泄露** | 等于公开仓库可被任意改写。别贴聊天、别写进文件、别提交进仓库；怀疑泄露就去 GitHub → Settings → Developer settings 立即 Revoke |
| **别把笔记放进站点目录** | `tech_doc` 是公开仓库，`提交笔记.bat` 第 4 步会在那里执行 `git add -A` |
| **提交历史带邮箱** | ~~公开仓库的每次提交都会公开你的 Git 邮箱~~ → **已处理，见下方「邮箱匿名化」** |

### 邮箱匿名化（已完成）

原先公开仓库的 9 个提交都带真实邮箱 `zhangspead0@outlook.com`。现已全部改为 GitHub 匿名邮箱：

```bat
:: 已在本仓库、my-tech-notes、my-algo-notes-dir 三处设好（只影响各自仓库）
git config user.email "174760772+godspead0@users.noreply.github.com"
```

**已完成的操作**：改写站点仓库的 9 个提交历史（`git filter-branch --env-filter`）→ 强制推送。
改写后 `git diff` 验证**文件内容零变化**，只动了提交元数据。

**仍有残留（重要）**：GitHub 会保留被"抛弃"的旧提交，**通过旧的 SHA 仍能直接打开**，
例如 `github.com/your-name/godspead0.github.io/commit/c77a4fe`。
不过：

- 分支的正常浏览路径（Commits 页面、`git log`、克隆）**已完全看不到旧提交** ✅
- 旧 SHA 不对外公开，只有当时看过仓库的人才可能知道
- 要**彻底**清除只有两条路：① 请 GitHub Support 清理缓存提交；② 删除并重建该仓库

> 另外建议去 GitHub → Settings → Emails 勾选
> **Keep my email addresses private** 和 **Block command line pushes that expose my email**，
> 从源头防止再次泄露。

### 网络：git 推送需要走代理

实测发现 `github.com`（20.205.243.166）**直连被阻断**，而 `api.github.com` 通畅，
系统代理在 `127.0.0.1:7892` 运行但 git 默认不走它。表现为推送报
`Failed to connect to github.com port 443`。

临时解决办法（不改全局配置）：

```bat
cd /d D:\vscode_test_all\tech_doc
git -c http.proxy=http://127.0.0.1:7892 push origin main
```

若想一劳永逸（代理关闭时需改回）：

```bat
git config --global http.proxy http://127.0.0.1:7892
:: 取消：git config --global --unset http.proxy
```

---

## 八、相关文件位置

| 内容 | 路径 |
| --- | --- |
| 站点源码仓库 | `D:\vscode_test_all\tech_doc`（公开，推 `main`） |
| SPA 源码 | `D:\vscode_test_all\tech_doc\notes-manager` |
| 一键提交脚本 | `D:\vscode_test_all\tech_doc\提交笔记.bat` |
| 离线自测 | `cd notes-manager && npm run smoke`（102 项断言，无需浏览器） |

> 改了网站源码后需要重新构建部署，推送到 `main` 后由 GitHub Actions 自动发布（约 1-2 分钟）。
> 只改笔记的话，点网站「同步」即可立刻看到，无需等待。
