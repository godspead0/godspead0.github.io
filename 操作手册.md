# 操作手册

> 个人在线笔记收纳与打卡系统 · 完整使用流程
> 线上地址：<https://godspead0.github.io/>

---

## 速查卡

| 我要…… | 去哪里 |
| --- | --- |
| 写技术笔记 | `D:\vscode_test_all\godspead0_understand\全栈\` |
| 写算法笔记 | `D:\vscode_test_all\test_algorithm\` |
| 提交到 GitHub | 双击 `D:\vscode_test_all\tech_doc\提交笔记.bat` |
| 看最新笔记 | 网站右上角 **同步** |
| 今日打卡 | 网站右上角 🔥（每天一次） |
| 切技术/算法 | 左侧栏 **仓库** |
| 备份全部 | 网站右上角 **导出** |

---

## 一、首次配置（只做一次，约 3 分钟）

### 1.1 创建能访问两个仓库的 Token

1. 浏览器打开 <https://github.com/settings/personal-access-tokens/new>
2. **Token name**：填 `notes-manager`
3. **Expiration**：建议 1 年（到期需重建）
4. **Repository access** → 选 **Only select repositories** → 勾选：
   - ✅ `godspead0_understand`（技术笔记）
   - ✅ `godspead0_algorithm`（算法笔记）
5. **Permissions** → 展开 **Repository permissions** → 找到 **Contents** → 设为 **Read and write**
6. 拉到底点 **Generate token** → 复制那串 `github_pat_...`

> ⚠️ Token 只显示一次，请自己保存好。**不要**提交进任何仓库文件、也不要发给别人——
> 它只存在浏览器本地 `localStorage` 里。

### 1.2 在网站里填配置

打开 <https://godspead0.github.io/>，第一次访问会自动弹出「连接 GitHub 数据仓库」；
否则点右上角 **⚙ 连接设置**。

弹窗顶部有 **技术 / 算法** 两个页签，**两个都要填**：

> ⚠️ 出于隐私考虑，网站**不再预填** Owner / Repo / Branch / 笔记目录
> —— 因为这些默认值会显示给任何打开网站的人。请按下表手动输入，**每台设备只需填一次**。

| 页签 | Owner | Repo | Branch | 笔记目录 | Token |
| --- | --- | --- | --- | --- | --- |
| **技术** | `godspead0` | `godspead0_understand` | `master` | `全栈` | 粘贴上面那串 |
| **算法** | `godspead0` | `godspead0_algorithm` | `main` | **留空** | 粘贴**同一串** |

> **技术页签的「笔记目录」务必填 `全栈`**：留空表示扫描整个仓库，
> 会把 `origin/` 里混进来的说明文档也当成笔记。

每个页签填完，各自点一次 **「测试并保存」**。看到绿色的：

```text
✅ 连接正常
仓库：godspead0/godspead0_understand（私有） · 分支：master · 写入权限：可写 ✅
```

才算成功。**若显示「只读 ❌」**，说明第 5 步 Contents 权限没设成 Read and write，回 GitHub 改 Token。

两个页签都测通后，点右上角 **✕** 关闭，网站会自动开始同步。

---

## 二、日常使用（每天的主流程）

### 2.1 写笔记——三选一

**方式 A：用 Obsidian 本地写（推荐）**

| 笔记类型 | 打开这个文件夹 | 分类怎么来 |
| --- | --- | --- |
| 技术 | `D:\vscode_test_all\godspead0_understand\全栈\` | 一级子文件夹名 = 分类，如 `前端部分\Vue.md` → 分类「前端部分」 |
| 算法 | `D:\vscode_test_all\test_algorithm\` | 同上，如 `力扣\二分查找.md` → 分类「力扣」 |

- 放在笔记目录根下也认（会归入「未分类」）
- 最多递归 6 层子目录
- 标题优先取正文里的 `# 标题`；正文为空则用文件名

**方式 B：在网站里写**

点右上角 **+ 新建** → 填标题 / 分类 / 标签 / 正文 →
注意有一栏 **「保存到仓库」**，下拉可选 `技术` 或 `算法`（决定这篇写进哪个库）→
点 **创建并同步**。

**方式 C：导入已有 `.md`**

点 **导入** → 选文件，或直接把整个文件夹拖进窗口。会写进当前选中的仓库。

### 2.2 提交到 GitHub

双击 `D:\vscode_test_all\tech_doc\提交笔记.bat`

```text
请输入本次提交说明（直接回车使用默认）：写完 MySQL 索引
```

然后它会自动跑三步（一次输入，两个笔记仓库共用这条说明）：

```text
[1/3] 同步技术笔记 ...
      [完成] 技术笔记 已推送。
[2/3] 同步算法笔记 ...
      [完成] 算法笔记 已推送。
[3/3] 检查站点源码 ...
```

- 某个仓库**没改动**会显示「没有检测到改动，跳过」，属正常
- 每步内部都是 `git add -A` → `commit` → `pull --rebase` → `push`
- 出现冲突会停下来并打印具体命令，按提示手动处理即可
- 跑完停在 `按任意键继续`，关掉窗口即可

### 2.3 网站上看最新笔记

回到网站，点右上角 **⟳ 同步**（约 1-3 秒）。
网站直连 GitHub 读取数据，**不需要**等站点部署完成。

### 2.4 打卡

右上角 🔥 按钮：

- **点一下** = 今日打卡 +1，按钮变绿显示「已打卡」；**每天只允许一次**（之后按钮置灰）
- **不用手动点**：当天首次新建 / 修改 / 导入笔记会自动 +1，
  并提示「已自动打卡 +1（笔记已创建）」
- 撤销 / 补卡在右侧仪表盘：可撤销今日打卡；点热力图上某一天可给**过去的某天补卡**
  （今天不允许重复补）

---

## 三、找笔记（侧边栏 + 搜索）

左侧栏从上到下：

```text
全部笔记            ← 清空所有筛选
当前筛选            ← 有筛选时才出现，点「清除筛选」一键还原
仓库                ← 技术 / 算法（一级分类），点一下只看这个库
分类                ← 文件夹名，随所选仓库联动
标签                ← 标签云，多选为 AND（同时满足）
时间线归档          ← 2026年9月 (4)，可折叠展开到具体笔记
```

**筛选可以叠加**：先点「算法」→ 再点分类「洛谷」→ 再看某个月份。

**搜索**：顶部搜索框**边打边筛**（不用回车）；右侧的 **搜索按钮**点击后会把光标
聚焦回输入框，方便继续改词；命中数量显示在按钮旁（如 `3 条`）。
按 `/` 或 `Ctrl+K` 也可直接聚焦搜索框。

右侧仪表盘显示：笔记总数、正文字数、最长连续打卡、今日打卡、打卡热力图。

---

## 四、其它操作

| 想做什么 | 怎么做 |
| --- | --- |
| 改一篇笔记 | 点卡片上的 ✏️；或在卡片上打开阅读页后点编辑 |
| 删除 | 卡片悬停 → 🗑；或列表里勾选多篇后批量删除 |
| 下载单篇 | 卡片悬停 → ⬇ |
| 全站备份 | 右上角 **导出** → 打包 `zip`（含全部笔记 + `checkins.json` + `categories.json`） |
| 换深浅色 | 右上角 ☀ / 🌙 |
| 换设备 | 新设备需重新填一次 Token（凭据存在浏览器本地，不同步） |
| 换 Token | 连接设置 → 对应页签改 Token → 测试并保存 |
| 彻底清除本机数据 | 连接设置 → 左下「清除凭据」→ 确认。会删掉 Token **以及本机缓存的笔记正文与打卡记录**（GitHub 上的笔记不受影响）。**在公共电脑用完请务必点它。** |

---

## 五、排错对照表

| 现象 | 原因与处理 |
| --- | --- |
| 「Token 无效或已过期」 | Token 被吊销 / 复制不全 / 过期。重建并重填**两个页签** |
| 「仓库或路径不存在」 | fine-grained Token **没勾选**那个仓库时 GitHub 返回 404（不是 403）。回 Token 设置把两个仓库都勾上 |
| 显示「只读 ❌」 | Contents 权限不是 Read and write |
| 算法仓库一篇都没有 | `test_algorithm` 里目前只有 `.cpp` 没有 `.md`，所以是空的；写一篇 `.md` 进去，或在网站切到「算法」后新建 |
| 某一类笔记不出现 | 检查该仓库页签的「笔记目录」：技术必须是 `全栈`，算法必须**留空** |
| 笔记数量对不上 | 只统计 `.md` / `.markdown`；两个仓库里的代码文件不会被当成笔记 |
| 打卡点不动 | 今天已经打过了（每天限一次）；补卡请在热力图上选过去的日期 |
| 保存冲突 409 | 同一文件在别处被改过。点「同步」拉最新版再改 |
| 单篇读取失败 | 文件 >1MB，前端会自动改走 `download_url` raw 通道 |
| 网站打不开 / 样式乱 | 刷新页面（`Ctrl + F5`） |

---

## 六、数据结构速查

### 6.1 两个数据仓库

| | 技术 | 算法 |
| --- | --- | --- |
| 仓库 | `godspead0/godspead0_understand`（私有） | `godspead0/godspead0_algorithm`（私有） |
| 本地克隆 | `D:\vscode_test_all\godspead0_understand` | `D:\vscode_test_all\test_algorithm` |
| 分支 | `master` | `main` |
| 扫描目录 | `全栈/`（递归 6 层） | 仓库根目录（递归 6 层） |
| 网站里的一级分类 | 技术 | 算法 |

### 6.2 文件命名与元数据

```text
{id}_{slug}.md          # 网站里新建的笔记（id = 13 位毫秒时间戳，slug 保留中文）
checkins.json           # { "YYYY-MM-DD": count, ... }   ← 自动创建
categories.json         # 分类 / 标签配色元数据          ← 自动创建
```

> `checkins.json` 与 `categories.json` 存放在**第一个已配置的仓库的根目录**（通常是技术仓库根目录）。

### 6.3 网站只展示什么

| | 技术仓库 | 算法仓库 |
| --- | --- | --- |
| 「笔记目录」设置 | `全栈` | 留空 |
| 实际扫描范围 | **仅 `全栈/` 子树内** | **整个仓库** |
| 展示的文件类型 | 只展示 `.md` / `.markdown` | 只展示 `.md` / `.markdown` |

- `origin/` 下的 `.cpp`、`.png`，算法仓库的 `.cpp`、`.prob`、`.json` —— **都不会展示**
- ⚠️ 算法仓库因为笔记目录留空，**任意位置**的 `.md` 都会被当成笔记。
  以后若在算法仓库根目录放 `README.md`，它也会出现在网站里。
  想限定范围，就把该页签的「笔记目录」填成具体文件夹名（如 `笔记`）

### 6.4 分类与标签的来源

- **分类** = 笔记所在的一级子文件夹名（技术：`全栈/前端部分/Vue.md` → 「前端部分」）
- **标签** = 笔记 frontmatter 里的 `tags`，或在网站编辑器里添加
- 两者都可以在网站里改，改完会自动写回对应仓库

---

## 七、安全须知

**别人能不能看到我的笔记？——不能。** 三层隔离：

1. 网站程序是公开的，但**只是一段空壳**，本身不含任何笔记
2. 笔记数据在两个**私有仓库**里，匿名访问一律 404
3. 打开数据的钥匙（Token）只存在**你自己浏览器**的 `localStorage`

没有 Token 的人打开网站，只会看到「连接设置」弹窗。

### 但要注意这几点

| 风险 | 说明 |
| --- | --- |
| **共用电脑** | 谁用了你的浏览器 = 能看到全部笔记。用完请点「清除凭据」——它会同时抹掉 Token、**笔记正文缓存**和打卡缓存 |
| **Token 泄露** | 等于全部笔记可读 + 可改 + 可删。别贴聊天、别写进文件、别提交进仓库；怀疑泄露就去 GitHub → Settings → Developer settings 立即 Revoke |
| **仓库名是公开的** | 公开产物里能看到 `godspead0_understand` / `godspead0_algorithm` 这两个名字（因为写在代码默认值里），但**看不到任何内容** |
| **别把笔记放进站点目录** | `tech_doc` 是公开仓库，`提交笔记.bat` 第 3 步会在那里执行 `git add -A`。**笔记只放 `全栈/` 和算法仓库目录**（已加 `.gitignore` 防呆） |
| **提交历史带邮箱** | ~~公开仓库的每次提交都会公开你的 Git 邮箱~~ → **已处理，见下方「邮箱匿名化」** |

### 邮箱匿名化（已完成）

原先公开仓库的 9 个提交都带真实邮箱 `zhangspead0@outlook.com`。现已全部改为 GitHub 匿名邮箱：

```bat
:: 已在本仓库、godspead0_understand、test_algorithm 三处设好（只影响各自仓库）
git config user.email "174760772+godspead0@users.noreply.github.com"
```

**已完成的操作**：改写站点仓库的 9 个提交历史（`git filter-branch --env-filter`）→ 强制推送。
改写后 `git diff` 验证**文件内容零变化**，只动了提交元数据。

**仍有残留（重要）**：GitHub 会保留被"抛弃"的旧提交，**通过旧的 SHA 仍能直接打开**，
例如 `github.com/godspead0/godspead0.github.io/commit/c77a4fe`。
不过：

- 分支的正常浏览路径（Commits 页面、`git log`、克隆）**已完全看不到旧提交** ✅
- 旧 SHA 不对外公开，只有当时看过仓库的人才可能知道
- 要**彻底**清除只有两条路：① 请 GitHub Support 清理缓存提交；② 删除并重建该仓库

> 另外建议去 GitHub → Settings → Emails 勾选
> **Keep my email addresses private** 和 **Block command line pushes that expose my email**，
> 从源头防止再次泄露。

### 网络：git 推送需要走代理

实测发现 `github.com`（20.205.243.166）**直连被阻断**，而 `api.github.com` 通畅，
系统代理在 `127.0.0.1:7892` 运行但 git 默认不走它。表现为推送报
`Failed to connect to github.com port 443`。

临时解决办法（不改全局配置）：

```bat
cd /d D:\vscode_test_all\tech_doc
git -c http.proxy=http://127.0.0.1:7892 push origin main
```

若想一劳永逸（代理关闭时需改回）：

```bat
git config --global http.proxy http://127.0.0.1:7892
:: 取消：git config --global --unset http.proxy
```

---

## 八、相关文件位置

| 内容 | 路径 |
| --- | --- |
| 站点源码仓库 | `D:\vscode_test_all\tech_doc`（公开，推 `main`） |
| SPA 源码 | `D:\vscode_test_all\tech_doc\notes-manager` |
| 一键提交脚本 | `D:\vscode_test_all\tech_doc\提交笔记.bat` |
| 离线自测 | `cd notes-manager && npm run smoke`（102 项断言，无需浏览器） |

> 改了网站源码后需要重新构建部署，推送到 `main` 后由 GitHub Actions 自动发布（约 1-2 分钟）。
> 只改笔记的话，点网站「同步」即可立刻看到，无需等待。
